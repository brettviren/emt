[defaults]
id = {name}-{version}

[package]
name = gcc
version = 4.8.2

[package]
name = worch
tags = buildtool, automation, validation

[package]
name = cmake
version = 2.8.12

[package]
name = mypkg
require: name=="gcc" and version >= "4.8.1"
require: "buildtool" in tags
require: id == "cmake-2.8.12"
